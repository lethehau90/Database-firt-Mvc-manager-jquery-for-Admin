@model IList<Web_config_v1.Models.Entity.Config>
@if (Model.Count > 0)
{
    foreach (var item in Model)
    {
        <meta name="description" content="@item.Description" />
        <meta name="keywords" content="@item.Keyword" />
    }
}