@model IList<Web_config_v1.Models.Entity.GroupNew>
@if (Model.Count > 0)
{
    <ul class="sub-menu">
        @foreach (var item in Model)
        {
            int Countgroup = Convert.ToInt32((Html.Action("Count_news_lever3", "CategoryNewsAdmin", new { Id = item.Id }).ToHtmlString()));
            
                <li id="menu-item-557" class="menu-item menu-item-type-post_type menu-item-object-page menu-item-has-children">
                    <a href="/san-pham/@item.Tag">@item.Name</a>
                    @{Html.RenderAction("Muti_Group_Menu_2", "Nav_News", new { Id = item.Id });}
                </li>
           
        }
    </ul>
}
