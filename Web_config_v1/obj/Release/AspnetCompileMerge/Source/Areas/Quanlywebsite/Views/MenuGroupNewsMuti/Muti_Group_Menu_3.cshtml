@model IList<Web_config_v1.Models.Entity.GroupNew>
@if (Model.Count > 0)
{
    <ul class="nav nav-third-level" style="padding-left: 20px;">
        @foreach (var item in Model)
        {
            <li>
                <a href="@Url.Action("Read", "CategoryNewsDetailsAdmin", new { tag = @item.Tag })">@item.Name</a>
            </li>
        }

    </ul>
}