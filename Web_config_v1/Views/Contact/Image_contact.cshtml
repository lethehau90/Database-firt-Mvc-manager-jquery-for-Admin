@model IList<Web_config_v1.Models.Entity.Advertise>
    @if (Model.Count > 0)
    {
        <a href="@Model[0].Link" target="@Model[0].Target">
            <img src="@Model[0].Image" alt="@Model[0].Name" />
        </a>
    }